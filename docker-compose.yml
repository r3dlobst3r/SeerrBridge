version: '3'

services:
  seerrbridge:
    build:
      context: .
      args:
        CACHEBUST: ${CACHEBUST:-$(date +%s)}  # Uses current timestamp
    ports:
      - "8777:8777"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: always
